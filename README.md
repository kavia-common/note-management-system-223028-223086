# note-management-system-223028-223086

This workspace contains the Notes Backend (Express) service located at `notes_backend/`.

- Run service: `npm start` inside `notes_backend/`
- API docs: `/docs` on the running server (port 3001 by default)